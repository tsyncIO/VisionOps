You are an expert system architect planning a visual diagram.

You are given extracted concepts, relationships, and an abstraction strategy.
Construct a formal DiagramSpec for rendering a REAL-WORLD PROJECT WORKFLOW PROCESS.

CRITICAL STRUCTURAL RULES:
1. NEVER create a generic star topology or hub-and-spoke diagram with a single central topic node pointing outwards to isolated concept spokes.
2. Build a DIRECTIONAL PROCESS PIPELINE (flowchart) representing a real software engineering process or execution workflow.
3. Map the ACTUAL EXTRACTED CONCEPTS into the sequential workflow nodes from left to right.
   For example, if concepts are [Command Time, Command Trace, gprof, Coverage Testing, Performance Tuning], map them to sequential workflow steps:
   [Step 1: Timing (Command Time)] --> [Step 2: System Trace (Command Trace)] --> [Step 3: Call Graph (gprof)] --> [Step 4: Test Coverage (Coverage Testing)] --> [Step 5: Refactoring (Performance Tuning)].
4. Set 'layout' to 'flowchart'.
5. Each DiagramNode must incorporate an extracted concept name:
   - id: URL-safe unique concept ID (e.g. 'command_time', 'command_trace', 'gprof', 'coverage_testing', 'performance_tuning')
   - label: Concrete step label using the concept name (e.g. '1. Execution Timing (Command Time)', '2. Call Graph Analysis (gprof)')
   - type: 'input', 'process', or 'output'
6. Each DiagramEdge must have an active verb label explaining the real transition (e.g. 'measures runtime', 'isolates hot functions', 'verifies branch coverage', 'drives code refactoring').

Important: Ensure all edge source and target IDs match valid concept IDs defined in the nodes list.
